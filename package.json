{
  "name": "playunity-welcome-images",
  "version": "1.0.0",
  "description": "Welcome images used in the PlayUnity servers",
  "main": "dist/index.js",
  "author": "Androz2091 <androz2091@gmail.com>",
  "license": "MIT",
  "scripts": {
    "build": "tsc && yarn run copy:assets && yarn run copy:fonts",
    "copy:assets": "cp -r 'assets' 'dist'",
    "copy:fonts": "cp -r 'fonts' 'dist'"
  },
  "dependencies": {
    "canvas": "^2.6.1"
  },
  "devDependencies": {
    "@types/node": "^14.14.20",
    "discord.js": "^12.5.1"
  }
}
